<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cskaoyan.mapper.goods.GoodsMapper">

  <select id="listPageGoods" resultMap="goodsMap">
    select
    id,
    goods_sn,
    name,
    category_id,
    brand_id,
    gallery,
    keywords,
    brief,
    is_on_sale,
    sort_order,
    pic_url,
    share_url,
    is_new,
    is_hot,
    unit,
    counter_price,
    retail_price,
    detail,
    add_time,
    update_time,
    deleted
    from cskaoyan_mall_goods
    limit #{limit}
    offset #{offset}
    order by #{sort} #{order}
  </select>
</mapper>